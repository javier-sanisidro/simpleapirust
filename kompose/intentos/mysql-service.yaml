kind: Service
apiVersion: v1
metadata:
  name: elserver
  labels:
      app: api
spec:
  type: ClusterIP
  selector:
    app: api
    tier: elserver
    ClusterIP: None
  ports:
  - port: 3306
    targetPort: 3306
    